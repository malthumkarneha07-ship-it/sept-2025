from collections import Counter 
str=["eat","tea","ate","nat","tan","bat"]
d=defaultdict(list)
for word in strs:
    s_word=" ".join(sorted(word))
    d[s_word].append(word)
result=[]
for key in sorted(d):
    result.append(d[key])
print(result)

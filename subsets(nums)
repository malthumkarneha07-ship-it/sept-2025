def subsets(nums):
    def generate(ind,ans,curr_subsets,nums):
        if(ind==len(nums))
        ans.append(curr_subsets.copy())
        return
    curr_subsets.append(nums[ind])
    generate(ind+1,ans,curr_subsets,nums)
    curr_subsets.pop()
    generate(ind+1,ans,curr_subsets,nums)
    ind=0
    ans[]
    curr_subsets=[]
    generate(ind,ans,curr_subsets,nums)
    return ans
nums=list(map(int,input().split()))
print(subsets(nums))
